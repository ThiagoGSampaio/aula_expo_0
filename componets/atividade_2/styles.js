import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },

    titulo:{
        fontSize: 30,
    },

    mensagem:{
        fontSize: 25,
        margin: 10,
    },

    viewmensagem:{
        borderWidth: 2,
        borderColor: 'black',
        borderRadius: 20,
    },
});

export default styles;